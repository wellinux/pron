<%= simple_form_for ([@firm, @firm.functions.build]), :html => { :class => 'form-horizontal' } do |c| %>

  <%= c.hidden_field :firm_id, :html_input => { :value => @firm } %>      
  <%= c.input :funcao %>
  <%= c.button :submit, :class => 'btn-primary' %>
<% end %>